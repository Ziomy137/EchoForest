namespace EchoForest.Core;

/// <summary>
/// Window display mode options available in the Settings screen.
/// </summary>
public enum WindowMode
{
    /// <summary>Resizable window; default on all platforms.</summary>
    Windowed,

    /// <summary>Borderless fullscreen at the desktop resolution; no resolution selection.</summary>
    BorderlessFullscreen
}
